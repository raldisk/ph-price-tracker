name: ph_price_tracker
version: "1.0.0"
config-version: 2

profile: ph_price_tracker

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ph_price_tracker:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
